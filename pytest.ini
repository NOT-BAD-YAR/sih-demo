[pytest]
markers =
    unit: fast pure-Python unit tests (no external services)
    structure: repo scaffolding / file-layout checks (no external services)
    contract: schema validation tests (Phase 1+)
    integration: tests that need running infra (Kafka / Postgres / docker)
    eval: Phase 9 metric/ablation tests
addopts = -ra